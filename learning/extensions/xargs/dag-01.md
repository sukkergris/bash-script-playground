# Extension 5 — xargs (parallel processing)

**Manual:** `man xargs`

**Hvad er det?**
`xargs` converter tekst fra stdin til arguments for andre commands. Bruges til parallel processing af lister.

**Status:** Under udarbejdelse  
**Kommer snart med:** Installation, basic syntax, parallellisering, null-delimiters, performance.
