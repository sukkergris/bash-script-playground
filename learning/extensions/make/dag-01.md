# Extension 6 — make (build automation)

**Manual:** `man make`

**Hvad er det?**
`make` er en task-runner og build-tool. Perfekt for at automatisere scripts, tester, deployments.

**Status:** Under udarbejdelse  
**Kommer snart med:** Installation, Makefile-syntax, targets, dependencies, variables, phony targets.
